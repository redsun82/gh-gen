# generated by gh-gen, do not edit
# fmt: off

from ghgen.syntax import uses
{{#actions}}


def {{id}}(
    *,
    {{#inputs}}
    {{name}}: str{{^required}} | None = None{{/required}},
    {{/inputs}}
):
    return uses(
        "{{{resolved_spec}}}"
    ).with_((
        {{#inputs}}
        ("{{id}}", {{name}}),
        {{/inputs}}
    )).name(
        "{{name}}"
    ){{#comment}}.comment(
        uses="{{.}}"
    ){{/comment}}{{#has_outputs}}.outputs(
        {{#outputs}}
        "{{.}}",
        {{/outputs}}
    ){{/has_outputs}}
{{/actions}}
